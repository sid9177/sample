SCRIPT_REAL("
import pandas as pd
import numpy as np
from statsmodels.tsa.arima_model import ARIMA

# Prepare your data
dates = pd.to_datetime(_arg1)
values = _arg2

# Fit the ARIMA model
model = ARIMA(values, order=(5,1,0))
model_fit = model.fit(disp=0)

# Forecast
forecast = model_fit.forecast(steps=5)[0]

return list(forecast)
", ATTR([Date Field]), SUM([Value Field]))
