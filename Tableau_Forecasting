SCRIPT_REAL('
    # This Python script returns a list of the same static value
    # The length of the list is determined by the length of the input list _arg1,
    # which is used here just to define how many times to repeat the static value.
    return [100.0] * len(_arg1)
',
[Some Field])  # Replace [Some Field] with a real field from your data source
