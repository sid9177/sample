SCRIPT_REAL('
import traceback
try:
    result = sum(_arg1)  # Assuming _arg1 is a list of numbers
    return result
except Exception as e:
    return str(e) + "\\n" + traceback.format_exc()  # Returning error as string for debugging
',
SUM([Your Field]))
